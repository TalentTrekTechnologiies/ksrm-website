[build]
command = "npm run build"
publish = "out"

[build.environment]
NODE_VERSION = "20"

# Static export - no serverless functions required
# The /out folder contains fully static HTML/CSS/JS

# Redirect /404.html for SPA-like 404 handling
[[redirects]]
from = "/*"
to = "/404.html"
status = 404
