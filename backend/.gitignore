# compiled output
/dist

# dependencies
/node_modules

# logs
logs
*.log
npm-debug.log*

# coverage
/coverage

# env
.env*
!.env.example
/generated/prisma

# Media Library local storage (uploaded originals + generated variants) -
# never committed, regenerable from a real storage backend in every
# non-local environment.
/storage
